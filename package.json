{
  "name": "attendance-tracker",
  "version": "1.0.0",
  "description": "Thiran Smart Hub - Attendance Matrix Tracker",
  "scripts": {
    "dev": "npx http-server -p 3000 -c-1",
    "build": "echo 'Static site - no build needed'",
    "start": "npx http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["attendance", "tracker", "firebase"],
  "keywords": [
    "attendance",
    "tracker",
    "firebase"
  ],
  "author": "Thiran",
  "license": "ISC",
  "dependencies": {}
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  }
}
